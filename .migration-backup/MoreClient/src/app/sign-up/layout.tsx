import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your account — clientMORE",
  description: "Create a clientMORE account and launch your bilingual AI support agent.",
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
